<%= form_for :user, url: user_login_path do |f| %>
  <%= f.label :login %>
  <%= f.text_field :login %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <br />
  <%= f.submit 'Submit', class: 'btn' %> <%= link_to 'or Sign Up', user_signup_path, class: 'btn btn-info' %>
<% end %>